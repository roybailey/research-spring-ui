# use embedded mode for all unit tests
# blank uri will use impermanent file, deleted after shutdown,
# or replace with valid file location path
neo4j.driver=org.neo4j.ogm.drivers.embedded.driver.EmbeddedDriver
neo4j.uri=
